{
  "name": "pm-cli-plugin-clickup",
  "version": "0.2.6",
  "description": "ClickUp plugin for pm-cli",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "rm -rf dist",
    "lint": "tsc -b --pretty false"
  },
  "dependencies": {
    "pm-cli-core": "workspace:^"
  }
}
