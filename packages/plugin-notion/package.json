{
  "name": "@jogi47/pm-cli-plugin-notion",
  "version": "0.2.1",
  "description": "Notion plugin for pm-cli",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "rm -rf dist",
    "lint": "eslint src",
    "prepack": "node -e \"const p=require('./package.json'); const d=JSON.stringify(p.dependencies||{}); if(d.includes('workspace:')) { console.error('ERROR: workspace: protocol found — use pnpm publish, not npm publish'); process.exit(1); }\""
  },
  "dependencies": {
    "@jogi47/pm-cli-core": "workspace:^",
    "@notionhq/client": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
