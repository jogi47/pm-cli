{
  "name": "@jogi47/pm-cli-plugin-trello",
  "version": "0.2.4",
  "description": "Trello plugin for pm-cli",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "rm -rf dist",
    "lint": "tsc -b --pretty false"
  },
  "dependencies": {
    "@jogi47/pm-cli-core": "workspace:^"
  }
}
